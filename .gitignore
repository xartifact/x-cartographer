# dependencies
node_modules/

# testing
coverage/

# next.js
.next/
out/

# production
build/
dist/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# bun：bun.lock **必须入库**——CI 与 Dockerfile 都用 `bun install --frozen-lockfile`，
# 该模式要求存在且最新的锁文件；忽略它会让 CI 直接失败（实测 run 35615634754：
# "lockfile had changes, but lockfile is frozen"）。同时它是 ci.yml 缓存 key 的输入
# （hashFiles('bun.lock')），不入库则缓存永不命中。

# PGlite local data
data/pglite/
apps/web/data/pglite

# IDE
.idea/
.swarm/
SWARM_PLAN.json
SWARM_PLAN.md
apps/server/data
apps/web/.tanstack

# 会话/代理产物（本地不入库）
.agents/
.codex/
